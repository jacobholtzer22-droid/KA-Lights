{
  "noindex": true,
  "_note": "Preview state. Every route is served with X-Robots-Tag noindex, nofollow (vercel.json) and a robots noindex meta tag (app/layout.tsx). Set noindex to false and delete the vercel.json header only at DNS cutover. The launch gate blocks production builds while this is true."
}
